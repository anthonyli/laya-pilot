# Roadmap

[English](ROADMAP.en.md)

以下是**计划**，不是当前已交付功能；没有承诺发布日期。README 的安装和命令只描述已实现能力。

| 优先级 | 计划 | 完成标准 |
| --- | --- | --- |
| P0 | 浏览器驱动接口 | 将页面操作、事件采集、截图、追踪从 Playwright API 中抽象出来；生成与回放都通过同一驱动契约运行 |
| P0 | browser-use 适配器 | 可通过配置选择；在公开 demo 上完成生成、回放和失败归因，达到与 Playwright 路径相同的断言语义 |
| P0 | 决策服务接口与 Jev API 适配器 | 可通过配置选择；验证候选映射、概率、超时、鉴权和错误处理；不依赖本地 Laya 包 |
| P1 | 更多前端组件适配 | 扩展原生表单、自定义下拉、虚拟列表、更多弹窗结构；每类都有公开 demo 与浏览器回归测试 |
| P1 | 生成策略配置 | 用户定义允许的增删改查范围、测试数据规则及清理策略；生成器不会猜测未授权的业务写入 |
| P1 | 多角色与审批流程 | 支持明确的测试账号配置和角色切换，记录每一步的操作者与断言证据 |
| P2 | 用例编辑与迁移 | 在人修改 Excel 测试步骤后，提供可审查的重新绑定流程，而不是静默执行旧隐藏步骤 |

实现顺序以实际需求和适配器契约测试为准。当前 `--browser-provider` 只接受 `playwright`，决策 `--provider` 只接受 `local` 或 `api`（均使用现有 Laya 决策协议）；未实现的值会直接报错。
